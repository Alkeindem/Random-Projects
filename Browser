/*/ Doc: This file is intended to be a kind-of a local browser for .txt files. /*/

// Including Block
#include <stdio.h>
#include <stdlib.h>

int main()
{
  char search[50];
  printf("Ingrese una entrada de un maximo de 50 caracteres\n");
  fgets(search, sizeof(search), stdin);
  return 1;
}
